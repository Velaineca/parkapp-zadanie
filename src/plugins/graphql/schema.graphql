schema {
  query: Query
  mutation: Mutation
}

type Query {
  me: User
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
}

type User {
  id: ID!
  email: String!
  name: String
  gates: [String!]!
}

type AuthPayload {
  token: String!
  user: User!
}